# 17 Branching and Release

## 1. Branching
- `main`: Stable production branch.
- `feature/...`, `bugfix/...`: Short-lived development branches.

## 2. Releases
- Use semantic versioning (SemVer).
- Tag releases in Git.
- Maintain a `CHANGELOG.md` (managed in `docs/`).
