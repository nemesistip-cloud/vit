{
  "contracts": [
    {
      "name": "DatabaseSession",
      "interface": "AsyncSessionLocal",
      "inputs": [],
      "outputs": ["AsyncSession"],
      "owner_system": "Database System",
      "dependencies": ["sqlalchemy"],
      "description": "Primary database session factory for all async database operations."
    },
    {
      "name": "GetDBDependency",
      "interface": "get_db",
      "inputs": [],
      "outputs": ["AsyncGenerator[AsyncSession, None]"],
      "owner_system": "Database System",
      "dependencies": ["DatabaseSession"],
      "description": "FastAPI dependency for injecting database sessions into route handlers."
    },
    {
      "name": "AIInference",
      "interface": "call_ai",
      "inputs": ["prompt: str", "**kwargs"],
      "outputs": ["str"],
      "owner_system": "AI System",
      "dependencies": [],
      "description": "Main entry point for AI text generation across all agents and services."
    },
    {
      "name": "AIInferenceWithProvider",
      "interface": "call_ai_with_provider",
      "inputs": ["prompt: str", "**kwargs"],
      "outputs": ["Tuple[str, str]"],
      "owner_system": "AI System",
      "dependencies": ["AIInference"],
      "description": "AI inference call that also returns the provider used."
    },
    {
      "name": "BackgroundTaskQueue",
      "interface": "app.worker.tasks",
      "inputs": ["Any"],
      "outputs": ["Any"],
      "owner_system": "Task System",
      "dependencies": ["celery/rq"],
      "description": "Entry points for scheduling asynchronous background tasks."
    }
  ]
}
