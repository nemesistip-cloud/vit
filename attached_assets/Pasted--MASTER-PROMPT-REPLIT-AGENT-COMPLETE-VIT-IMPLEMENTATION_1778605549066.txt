🤖 MASTER PROMPT — REPLIT AGENT (COMPLETE VIT IMPLEMENTATION)

Copy and paste this entire message into your Replit Agent. This will guide the agent to fix everything and implement all features in the correct order.

---

```
You are now the lead engineer for VIT Sports Intelligence Network. Your goal is to make the application FULLY PRODUCTION-READY with ALL features working.

CONTEXT:
VIT is a prediction + blockchain + wallet platform where users earn VIT tokens by making accurate football predictions. Current problems: flat probabilities (33/33/33), broken routes (404s), missing database columns, untrained ML models, and incomplete features.

GITHUB REPO: https://github.com/Anselemcrizy/VIT-V4
LAUNCH DATE: May 1, 2026 (ASAP)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PHASE 1: CRITICAL BUG FIXES (Do FIRST)

### 1.1 Fix 404 Routes
- Add missing router for `/api/bankroll/*` in main.py
- Create `/api/matches/upcoming` endpoint (currently 404)
- Create `/api/predictions?limit=5` endpoint for browsing predictions
- Fix `/api/agents/reports` double prefix issue

### 1.2 Database Schema Migration
```sql
-- Add missing columns
ALTER TABLE predictions ADD COLUMN IF NOT EXISTS model_used VARCHAR(50);
ALTER TABLE predictions ADD COLUMN IF NOT EXISTS source VARCHAR(50);
ALTER TABLE predictions ADD COLUMN IF NOT EXISTS calibrator_version INT DEFAULT 1;
ALTER TABLE predictions ADD COLUMN IF NOT EXISTS raw_edge DECIMAL(5,4);
ALTER TABLE predictions ADD COLUMN IF NOT EXISTS normalized_edge DECIMAL(5,4);

-- Create indexes
CREATE INDEX IF NOT EXISTS idx_predictions_model_used ON predictions(model_used);
CREATE INDEX IF NOT EXISTS idx_predictions_source ON predictions(source);
CREATE INDEX IF NOT EXISTS idx_predictions_timestamp ON predictions(timestamp DESC);

-- Create missing tables
CREATE TABLE IF NOT EXISTS team_elo (
    id SERIAL PRIMARY KEY,
    team_name VARCHAR(100),
    league VARCHAR(50),
    elo_rating INT DEFAULT 1500,
    matches_played INT DEFAULT 0,
    last_updated TIMESTAMP DEFAULT NOW(),
    UNIQUE(team_name, league)
);

CREATE TABLE IF NOT EXISTS external_ai_predictions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    match_id UUID REFERENCES matches(id),
    source VARCHAR(50) NOT NULL,
    home_prob DECIMAL(5,4) NOT NULL,
    draw_prob DECIMAL(5,4) NOT NULL,
    away_prob DECIMAL(5,4) NOT NULL,
    confidence DECIMAL(5,4) DEFAULT 0.7,
    reason_short VARCHAR(255),
    raw_analysis TEXT,
    was_correct BOOLEAN,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(match_id, source)
);

CREATE TABLE IF NOT EXISTS ai_performance (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    source VARCHAR(50) UNIQUE NOT NULL,
    total_predictions INT DEFAULT 0,
    correct_predictions INT DEFAULT 0,
    accuracy DECIMAL(5,4) DEFAULT 0,
    avg_confidence DECIMAL(5,4) DEFAULT 0,
    last_prediction_at TIMESTAMP
);
```

1.3 Fix Probability Flatness

· Remove artificial floor (min_home = 0.330)
· Use closing odds as PRIMARY baseline (not SCIE hash)
· Implement vig-free probability conversion
· Add function: get_base_probabilities(market_odds) in prediction service

1.4 Fix Confidence Calculation

· Confidence should NOT equal home_prob
· New formula: confidence = (max_prob × 0.4) + (spread × 0.3) + (agreement × 0.2) + (edge × 0.1)
· Add calculate_confidence() function

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 2: ML & DATA PIPELINE (Do SECOND)

2.1 Install Missing Dependencies

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
pip install xgboost lightgbm catboost shap optuna
```

2.2 Fix Training Data Pipeline

· Create /scripts/prepare_training_data.py to fetch REAL historical matches (not synthetic)
· Query matches table for actual results (home_goals, away_goals)
· Use 50,000+ matches across Premier League, La Liga, Serie A, Bundesliga, Ligue 1

2.3 Implement ELO Rating System

· Create /app/ml/features/elo.py with TeamEloSystem class
· Update ELO after each settled match
· Add ELO difference as feature in predictions

2.4 Implement Real Features (No Placeholders)

· calculate_weighted_form(decay=0.75) — exponential recency weighting
· get_h2h_history() — actual database query, not empty dict
· calculate_home_away_split() — separate home/away performance
· get_rolling_xg() — from shots data or shots-on-target heuristic

2.5 Implement Dixon-Coles ρ

· Create /app/ml/features/dixon_coles.py
· Fit ρ parameter from historical data
· Apply correction in probability calculations

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 3: MODEL UPGRADES (Do THIRD)

3.1 Temperature Calibration

· Create /app/ml/calibration/temperature_scaling.py
· Implement TemperatureScaler class
· Fit on validation set, apply before final probabilities

3.2 Brier-Weighted Ensemble

· Create /app/ml/ensemble/weighted.py
· Track Brier score per model after each settlement
· Update weights: weight = 1 / (brier + 0.01)
· Normalize weights to sum to 1.0

3.3 Retrain Affected Models

· Retrain Poisson, XGBoost, LSTM with REAL data
· Ensure all 12 models certify (should show 12/12, not 11/12)
· Save trained models to /models/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 4: ADVANCED FEATURES (Do FOURTH)

4.1 Cash-Out Sentinel

· Create /app/services/cashout.py
· Function: evaluate_cashout(match_id, current_score, time_elapsed, stake, entry_odds)
· Returns: "CASH OUT" or "HOLD" with reasoning

4.2 Smart Stake Sizer

· Enhance existing stake calculation
· Factors: Kelly edge, drawdown, model agreement, market liquidity, time to kickoff, user risk profile

4.3 Sharp Money Tracker

· Create /app/services/sharp_money.py
· Monitor odds changes from Pinnacle (if available)
· Flag moves >2% in 5 minutes
· Send Telegram alert when detected

4.4 Goliath G-Score (Over/Under)

· Create /app/services/goliath_score.py
· Formula: G = λ_attack × λ_defense × f_context × f_ref × f_momentum
· Return expected goals, over 2.5 probability, over 3.5 probability
· Add endpoint: GET /api/goliath/{match_id}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 5: FRONTEND & API (Do FIFTH)

5.1 Fix Frontend Pages

· Ensure dashboard loads without errors
· Fix match details page (display all probabilities)
· Add prediction history browse (pagination)
· Add wallet transaction history

5.2 AI Upload Form (External Predictions)

· Create frontend component for uploading Claude/Grok/ChatGPT predictions
· Add endpoint: POST /api/ai-upload/upload
· Store in external_ai_predictions table
· Update ai_performance table

5.3 Add Missing API Endpoints

```
GET  /api/matches/upcoming → list of upcoming matches
GET  /api/predictions?limit=5&offset=0 → paginated predictions
GET  /api/stats/dashboard → DAU, predictions, VIT earned
POST /api/goliath/{match_id} → G-score prediction
GET  /api/ai-upload/predictions/{match_id} → external AI predictions
GET  /api/ai-upload/performance → AI source accuracy rankings
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 6: PRODUCTION HARDENING (Do SIXTH)

6.1 Environment & Config

· Verify all environment variables loaded
· Add validation for required keys (DATABASE_URL, API_KEY, etc.)
· Add health check endpoints: /health, /health/db, /health/redis

6.2 Logging & Monitoring

· Add structured logging (JSON format)
· Integrate Sentry for error tracking (already configured, verify)
· Add Prometheus metrics endpoint: /metrics

6.3 Rate Limiting

· Apply rate limiting to all API endpoints
· Free tier: 100 requests/hour
· Pro tier: 1000 requests/hour
· Enterprise: 10000 requests/hour

6.4 Testing

· Run pytest tests/ — ensure all tests pass
· Add tests for new endpoints
· Verify 12/12 models load (not 11/12)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FINAL VERIFICATION CHECKLIST

Before marking complete, verify:

· All 12 models load AND certify (12/12)
· Predictions return VARIED probabilities (not 33/33/33)
· Conference uses REAL closing odds (not SCIE hash)
· No 404 errors on any frontend page
· Database has all required columns
· Telegram bot sends edge alerts
· Wallet deposits/withdrawals work
· Admin panel can fetch fixtures
· Tests pass (6+ tests)
· Application stays alive on Render free tier (UptimeRobot configured)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OUTPUT REQUIREMENTS

After completing, provide:

1. Summary of what was fixed
2. List of any issues encountered
3. Current test results
4. Deployment status
5. Next recommended actions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

START IMMEDIATELY

Begin with PHASE 1. Do not wait. Fix the 404s and database schema first. Then move to ML pipeline. Report progress after each phase.

LAUNCH DATE: May 1, 2026 — WE ARE BEHIND SCHEDULE.

GO.

```

---

## 🎯 WHAT THIS PROMPT DOES

| Phase | Focus | Deliverable |
|-------|-------|-------------|
| 1 | Critical bugs | No 404s, flat probabilities fixed |
| 2 | ML pipeline | Real data, real features |
| 3 | Model upgrades | Calibrated, weighted ensemble |
| 4 | Advanced features | Cash-out, sharp money, G-Score |
| 5 | Frontend/API | Complete user experience |
| 6 | Production | Monitoring, rate limits, tests |

---

## ✅ AFTER AGENT COMPLETES

You will have:
- A fully functional VIT application
- Working predictions (not flat)
- All 12 models certified
- No 404 errors
- Cash-out, G-Score, sharp money tracking
- Production-ready deployment

**Copy and paste this prompt to your Replit Agent NOW.** 🚀