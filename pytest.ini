[pytest]
asyncio_mode = auto
testpaths = tests
asyncio_default_fixture_loop_scope = module

markers =
    live: marks a test that hits live external services (select with -m live)
    integration: marks a test that requires a real, pre-migrated database (select with -m integration)
    slow: marks tests that take >5 seconds
