# VIT Contracts — ARCHIVED (Foundry)

    > **Status: Not deployed. Retained for reference only.**
    > Base L2 / Base Sepolia deployment has been removed from VIT Network scope.
    > Chain settlement is now handled by the standalone **vit-chain** node (Chain ID 7764).
    > See [vitnetwork/vit-chain](https://github.com/vitnetwork/vit-chain).

    ## Contracts (Foundry, solc 0.8.28)

    | Contract | Description |
    |----------|-------------|
    | `VITToken` | ERC-20 VIT token |
    | `UniversalOracle` | On-chain oracle aggregator |
    | `ShopManager` | Loyalty shop logic |
    | `LoyaltyVault` | Stake / vault logic |
    | `ElectoralOracle` | Electoral prediction oracle |

    These contracts will be revisited when VIT Chain mainnet launches and L1 settlement strategy is defined.

    **Do not deploy to any network without explicit VIT Network team approval.**
    